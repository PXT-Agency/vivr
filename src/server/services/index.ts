/**
 * Server-side business logic.
 *
 * Services are introduced in later phases (star-number domain service,
 * import service, reservation service). All business logic stays outside
 * React components.
 */
